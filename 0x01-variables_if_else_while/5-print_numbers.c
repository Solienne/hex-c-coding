#include <stdio.h>

/**
* main - prints single numbers os base ten < 110
*
* Return: Always 0 (Success)
*/
int main(void)
{

	int i = 0;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}

	printf("\n");

	return (0);
}
